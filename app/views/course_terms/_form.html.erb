<%= form_for(@course_term, html: {class: 'form-inline'}) do |f| %>
  <% if @course_term.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_term.errors.count, "error") %> prohibited this course_term from being saved:</h2>

      <ul>
        <% @course_term.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.select :season, CourseTerm.seasons.keys.to_a.map { |w| [w.humanize, w] }, class: 'form-control' %>
  </div>
  <div class="field form-group">
    <%= f.text_field :year, class: 'form-control' %>
  </div>
  <%= f.submit class: 'btn btn-primary' %>
  <%= link_to 'Cancel', course_terms_path, class: 'btn btn-default' %>
<% end %>
